"use strict";navigator.serviceWorker&&console.log("service worker is supported");const bubbles=document.querySelectorAll(".potion-bubbles .bubble");function bubbling(){for(let e=0;e<bubbles.length;e++)bubbles[e].style.animationDelay=randomTime(1,1.5,!0)}function randomTime(e,b,o){let n=Math.random()*(b-e+1)+e;return`${(n=o?-1*n:n).toFixed(2)}s`}bubbling();